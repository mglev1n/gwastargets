% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_column_map.R
\name{build_column_map}
\alias{build_column_map}
\title{Build a column rename map from per-cohort manifest values}
\usage{
build_column_map(
  col_chr = NA_character_,
  col_pos = NA_character_,
  col_rsid = NA_character_,
  col_effect_allele = NA_character_,
  col_other_allele = NA_character_,
  col_beta = NA_character_,
  col_se = NA_character_,
  col_p = NA_character_,
  col_eaf = NA_character_,
  col_n = NA_character_,
  col_n_cases = NA_character_,
  col_n_controls = NA_character_
)
}
\arguments{
\item{col_chr}{Column name mapping to \code{CHR}.}

\item{col_pos}{Column name mapping to \code{POS}.}

\item{col_rsid}{Column name mapping to \code{RSID}.}

\item{col_effect_allele}{Column name mapping to \code{EffectAllele}.}

\item{col_other_allele}{Column name mapping to \code{OtherAllele}.}

\item{col_beta}{Column name mapping to \code{B}.}

\item{col_se}{Column name mapping to \code{SE}.}

\item{col_p}{Column name mapping to \code{P}.}

\item{col_eaf}{Column name mapping to \code{EFF_ALL_FREQ}.}

\item{col_n}{Column name mapping to \code{N}.}

\item{col_n_cases}{Column name mapping to \code{N_CASES}.}

\item{col_n_controls}{Column name mapping to \code{N_CONTROLS}.}
}
\value{
A named character vector where names are source column names and
values are target harmonised names, or \code{NULL} if all arguments are \code{NA}.
}
\description{
Constructs a named character vector mapping non-standard column names
(as they appear in the raw summary statistics file) to harmonised names.
Each \verb{col_*} argument corresponds to a single harmonised column. Pass the
cohort-specific raw column name (a string) to override the default mapping,
or leave as \code{NA} to skip.

The returned vector is suitable for the \code{column_map} argument of
\code{\link[=harmonize_sumstats_headers]{harmonize_sumstats_headers()}}.
}
\examples{
build_column_map(col_eaf = "MY_FREQ", col_beta = "BETA_VAL")
# c("MY_FREQ" = "EFF_ALL_FREQ", "BETA_VAL" = "B")

build_column_map()
# NULL

}
