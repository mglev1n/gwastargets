% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_analyze_ivw.R
\name{meta_analyze_ivw}
\alias{meta_analyze_ivw}
\title{Inverse-variance weighted GWAS meta-analysis}
\usage{
meta_analyze_ivw(
  parquet_files,
  trait_type,
  chromosomes = 1:22,
  min_mac = 100,
  se_col = SE,
  beta_col = B,
  eaf_col = EAF,
  chr_col = CHR,
  pos_col = POS_38,
  rsid_col = RSID,
  ea_col = EffectAllele,
  oa_col = OtherAllele,
  n_col = N,
  effective_n_col = EffectiveN,
  case_n_col = CaseN,
  control_n_col = ControlN
)
}
\arguments{
\item{parquet_files}{Character vector of paths to prepared parquet files
(output of \code{\link[=prep_gwas]{prep_gwas()}}).}

\item{trait_type}{\code{"binary"} or \code{"quantitative"} (required).}

\item{chromosomes}{Integer vector of chromosomes to process. Default \code{1:22}.}

\item{min_mac}{Minimum minor allele count. Variants below this threshold are
excluded. Default \code{100}.}

\item{se_col}{Bare column name for SE. Default \code{SE}.}

\item{beta_col}{Bare column name for effect size. Default \code{B}.}

\item{eaf_col}{Bare column name for EAF. Default \code{EAF}.}

\item{chr_col}{Bare column name for chromosome. Default \code{CHR}.}

\item{pos_col}{Bare column name for position. Default \code{POS_38}.}

\item{rsid_col}{Bare column name for rsid. Default \code{RSID}.}

\item{ea_col}{Bare column name for effect allele. Default \code{EffectAllele}.}

\item{oa_col}{Bare column name for other allele. Default \code{OtherAllele}.}

\item{n_col}{Bare column name for total N. Default \code{N}.}

\item{effective_n_col}{Bare column name for effective N. Default
\code{EffectiveN}.}

\item{case_n_col}{Bare column name for case N. Default \code{CaseN}.}

\item{control_n_col}{Bare column name for control N. Default \code{ControlN}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns: \code{CHR}, \code{POS_38}, \code{RSID},
\code{EffectAllele}, \code{OtherAllele}, \code{B}, \code{SE}, \code{p_value}, \code{z_score}, \code{EAF},
\code{n_contributions}, \code{N}, \code{EffectiveN}, \code{Q}, \code{Q_df}, \code{Q_pval}, \code{I2}.
Binary traits additionally include \code{CaseN} and \code{ControlN}.
}
\description{
Performs a fixed-effects inverse-variance weighted (IVW) meta-analysis
across multiple cohort parquet files. Processes each chromosome via
\code{\link[=meta_analyze_chromosome]{meta_analyze_chromosome()}}, applies a minor allele count (MAC) filter,
and returns heterogeneity statistics (Q, I2) alongside the weighted effect
estimates.
}
\details{
IVW meta-analysis across cohorts
}
\examples{
\dontrun{
meta <- meta_analyze_ivw(
  parquet_files = c("study1.parquet", "study2.parquet"),
  trait_type    = "binary",
  chromosomes   = 1:22
)
}

}
