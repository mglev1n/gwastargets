% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_loci.R
\name{extract_loci}
\alias{extract_loci}
\title{Extract genome-wide significant independent loci}
\usage{
extract_loci(
  df,
  snp_col = RSID,
  chr_col = CHR,
  pos_col = POS_38,
  maf_col = EAF,
  beta_col = B,
  se_col = SE,
  p_col = p_value,
  p_threshold = 5e-08,
  build = 38,
  ...
)
}
\arguments{
\item{df}{A data frame of meta-analysis results (e.g. output of
\code{\link[=meta_analyze_ivw]{meta_analyze_ivw()}}).}

\item{snp_col}{Bare column name for rsid. Default \code{RSID}.}

\item{chr_col}{Bare column name for chromosome. Default \code{CHR}.}

\item{pos_col}{Bare column name for position. Default \code{POS_38}.}

\item{maf_col}{Bare column name for minor allele frequency. Default \code{EAF}.}

\item{beta_col}{Bare column name for effect size. Default \code{B}.}

\item{se_col}{Bare column name for SE. Default \code{SE}.}

\item{p_col}{Bare column name for p-value. Default \code{p_value}.}

\item{p_threshold}{Genome-wide significance threshold. Default \code{5e-8}.}

\item{build}{Genome build for gene annotation (\code{37} or \code{38}). Default \code{38}.}

\item{...}{Additional arguments forwarded to \code{\link[gwasRtools:get_loci]{gwasRtools::get_loci()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of independent lead variants with gene
annotations, or an empty tibble if no variants pass the threshold.
}
\description{
Filters a meta-analysis data frame to genome-wide significant variants,
identifies independent loci via \code{\link[gwasRtools:get_loci]{gwasRtools::get_loci()}}, and annotates
each lead variant with the nearest gene via \code{\link[gwasRtools:get_nearest_gene]{gwasRtools::get_nearest_gene()}}.
Returns an empty \code{\link[tibble:tibble]{tibble::tibble()}} when no variants pass the p-value
threshold.
}
\details{
Extract genome-wide significant loci
}
\examples{
\dontrun{
loci <- extract_loci(meta_results, p_threshold = 5e-8)
}

}
