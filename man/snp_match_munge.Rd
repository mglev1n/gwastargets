% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snp_match_munge.R
\name{snp_match_munge}
\alias{snp_match_munge}
\title{Match GWAS SNPs against a reference SNP list}
\usage{
snp_match_munge(
  sumstats,
  info_snp,
  strand_flip = TRUE,
  join_by_pos = TRUE,
  remove_dups = TRUE,
  match.min.prop = 0.2,
  return_flip_and_rev = FALSE
)
}
\arguments{
\item{sumstats}{A data frame of GWAS summary statistics. Must contain
columns \code{rsid}, \code{a0}, \code{a1}, and \code{beta}.}

\item{info_snp}{A data frame of reference SNPs. Must contain columns
\code{rsid}, \code{a0}, and \code{a1}.}

\item{strand_flip}{Logical; if \code{TRUE} (default), remove ambiguous SNPs
(A/T, C/G) and attempt strand flipping.}

\item{join_by_pos}{Logical; if \code{TRUE}, also join by position. Default
\code{TRUE}.}

\item{remove_dups}{Logical; if \code{TRUE} (default), remove duplicate
rsid entries after matching.}

\item{match.min.prop}{Minimum proportion of variants that must match.
Default \code{0.2}. An error is raised if fewer variants match.}

\item{return_flip_and_rev}{Logical; if \code{TRUE}, retain the \verb{_FLIP_} and
\verb{_REV_} indicator columns in the output. Default \code{FALSE}.}
}
\value{
A data frame of matched variants ordered by chromosome.
}
\description{
Matches variants in \code{sumstats} against \code{info_snp} by rsid, effect allele
(\code{a1}), and other allele (\code{a0}). Optionally removes ambiguous SNPs
(A/T and C/G), performs strand flipping, and reverses allele coding.
}
\details{
Match and munge SNPs against a reference panel
}
\examples{
\dontrun{
matched <- snp_match_munge(
  sumstats = my_sumstats,
  info_snp = hm3_snps
)
}

}
